# 快速部署指南

## 服务器信息
- **IP**: 109.244.63.71
- **用户**: root
- **项目目录**: /vol1/1000/docker/xiyiyun

## 一键检查命令

连接到服务器后，运行以下命令：

```bash
cd /vol1/1000/docker/xiyiyun && ./scripts/check-server-status.sh
```

## 如果服务未启动

```bash
cd /vol1/1000/docker/xiyiyun
docker compose -p xiyiyun -f docker-compose.prod.yml --env-file .env up -d --build
```

## 查看日志

```bash
# 查看所有服务
docker compose -p xiyiyun -f docker-compose.prod.yml logs -f

# 只看后端
docker compose -p xiyiyun -f docker-compose.prod.yml logs -f backend
```

## 访问地址（服务器本地）

- Web 前台: http://127.0.0.1:18080
- H5: http://127.0.0.1:18081
- 管理后台: http://127.0.0.1:18088

## 管理员登录

- 用户名: admin
- 密码: Admin@2026Secure（如果使用了本地生成的配置）

---

详细说明请查看：
- 完整验证清单: `服务器部署验证清单.md`
- 检查脚本: `scripts/check-server-status.sh`
