# 服务器远程管理说明

## 问题说明

当前 Claude 运行环境是隔离的 Linux VM，只有本地回环网络（127.0.0.1），**无法直接访问外网服务器**。这是安全设计，不是配置问题。

## 解决方案

我为你创建了可以在**你的本地电脑**上运行的管理脚本，通过 SSH 远程操作服务器。

---

## 🚀 快速开始

### 方式 1: 使用服务器管理工具（推荐）

在你的本地电脑终端运行：

```bash
cd /Users/bailang/Desktop/xy
./server-manager.sh
```

这个交互式工具提供菜单选项：
1. 查看服务状态
2. 查看容器日志
3. 启动所有服务
4. 重启所有服务
5. 停止所有服务
6. 查看环境配置
7. 运行完整检查
8. 进入服务器 Shell

### 方式 2: 一键检查脚本

```bash
cd /Users/bailang/Desktop/xy
./upload-and-check.sh
```

这个脚本会：
1. 自动上传检查脚本到服务器
2. 在服务器上执行完整检查
3. 显示所有服务状态

### 方式 3: 手动 SSH 连接

```bash
# 连接到服务器
ssh root@109.244.63.71

# 进入项目目录
cd /vol1/1000/docker/xiyiyun

# 运行检查
./scripts/check-server-status.sh
```

---

## 📋 常用命令速查

### 查看服务状态
```bash
ssh root@109.244.63.71 "cd /vol1/1000/docker/xiyiyun && docker compose -p xiyiyun -f docker-compose.prod.yml ps"
```

### 查看日志
```bash
ssh root@109.244.63.71 "cd /vol1/1000/docker/xiyiyun && docker compose -p xiyiyun -f docker-compose.prod.yml logs --tail=50"
```

### 启动服务
```bash
ssh root@109.244.63.71 "cd /vol1/1000/docker/xiyiyun && docker compose -p xiyiyun -f docker-compose.prod.yml up -d"
```

### 重启服务
```bash
ssh root@109.244.63.71 "cd /vol1/1000/docker/xiyiyun && docker compose -p xiyiyun -f docker-compose.prod.yml restart"
```

---

## 📁 已创建的文件

1. **server-manager.sh** - 交互式服务器管理工具
2. **upload-and-check.sh** - 一键上传并检查
3. **scripts/check-server-status.sh** - 服务器端检查脚本
4. **服务器部署验证清单.md** - 完整的检查清单
5. **快速部署指南.md** - 快速参考

---

## ⚠️ 注意事项

1. 这些脚本需要在**你的本地电脑**（Mac）上运行，不是在 Claude 环境中
2. 确保你的电脑可以 SSH 连接到服务器 109.244.63.71
3. 如果需要密码，脚本会提示你输入

---

## 🔧 如果你想让我帮你分析

由于我无法直接连接服务器，你可以：

1. 在本地运行 `./server-manager.sh` 选择"7. 运行完整检查"
2. 把输出结果复制给我
3. 我可以帮你分析问题并提供解决方案

或者直接把以下命令的输出发给我：

```bash
ssh root@109.244.63.71 "cd /vol1/1000/docker/xiyiyun && docker compose -p xiyiyun -f docker-compose.prod.yml ps && echo '---' && docker compose -p xiyiyun -f docker-compose.prod.yml logs --tail=30 backend"
```
