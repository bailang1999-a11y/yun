# 喜易云服务器部署验证清单

**服务器**: 109.244.63.71
**检查日期**: 2026年6月2日
**项目目录**: /vol1/1000/docker/xiyiyun

---

## 📋 部署前检查清单

### 1. 服务器环境准备
- [ ] 服务器可以访问（SSH 连接正常）
- [ ] Docker 已安装并运行
- [ ] Docker Compose 已安装
- [ ] 项目目录已创建：`/vol1/1000/docker/xiyiyun`
- [ ] 代码已部署到服务器

### 2. 环境配置检查
- [ ] `.env` 文件已创建（从 `.env.feiniu.example` 复制）
- [ ] MySQL root 密码已修改（不是 change_me）
- [ ] MySQL 用户密码已修改（不是 change_me）
- [ ] 管理员密码哈希已生成并配置
- [ ] 支付回调密钥已生成（64字符）
- [ ] 卡密加密密钥已生成（64字符）
- [ ] CORS 域名已配置为实际 HTTPS 域名（不是 example.com）
- [ ] HTTP_BIND 保持为 127.0.0.1

### 3. 端口配置检查
- [ ] WEB_PORT=18080
- [ ] H5_PORT=18081
- [ ] ADMIN_PORT=18088
- [ ] 端口没有被其他服务占用

---

## 🚀 部署步骤

### 步骤 1: 连接到服务器
```bash
ssh root@109.244.63.71
```

### 步骤 2: 进入项目目录
```bash
cd /vol1/1000/docker/xiyiyun
```

### 步骤 3: 检查环境配置
```bash
# 查看 .env 文件是否存在
ls -la .env

# 检查配置（不会显示敏感信息）
cat .env | grep -v PASSWORD | grep -v SECRET
```

### 步骤 4: 运行状态检查脚本
```bash
# 赋予执行权限
chmod +x scripts/check-server-status.sh

# 运行检查
./scripts/check-server-status.sh
```

### 步骤 5: 查看容器状态
```bash
docker compose -p xiyiyun -f docker-compose.prod.yml ps
```

### 步骤 6: 查看服务日志
```bash
# 查看所有服务日志
docker compose -p xiyiyun -f docker-compose.prod.yml logs -f

# 只查看后端日志
docker compose -p xiyiyun -f docker-compose.prod.yml logs -f backend

# 只查看最近 50 行
docker compose -p xiyiyun -f docker-compose.prod.yml logs --tail=50
```

---

## ✅ 服务验证清单

### 1. 容器运行状态
检查以下容器是否都在运行：
- [ ] mysql (数据库)
- [ ] redis (缓存)
- [ ] backend (后端 API)
- [ ] web (Web 前台)
- [ ] h5 (H5 端)
- [ ] admin (管理后台)

**验证命令**:
```bash
docker compose -p xiyiyun -f docker-compose.prod.yml ps
```

### 2. 端口监听检查
- [ ] 18080 端口监听中 (Web 前台)
- [ ] 18081 端口监听中 (H5)
- [ ] 18088 端口监听中 (管理后台)

**验证命令**:
```bash
netstat -tuln | grep -E "18080|18081|18088"
# 或
ss -tuln | grep -E "18080|18081|18088"
```

### 3. 服务访问测试
在服务器上测试：
```bash
# 测试 Web 前台
curl -I http://127.0.0.1:18080

# 测试 H5
curl -I http://127.0.0.1:18081

# 测试管理后台
curl -I http://127.0.0.1:18088

# 测试后端 API（如果有健康检查端点）
curl http://127.0.0.1:18080/api/health
```

- [ ] Web 前台响应正常
- [ ] H5 响应正常
- [ ] 管理后台响应正常
- [ ] 后端 API 响应正常

### 4. 数据库连接测试
```bash
docker compose -p xiyiyun -f docker-compose.prod.yml exec mysql mysql -uroot -p -e "SHOW DATABASES;"
```
- [ ] 数据库连接成功
- [ ] xiyiyun 数据库存在
- [ ] 表结构已初始化

### 5. Redis 连接测试
```bash
docker compose -p xiyiyun -f docker-compose.prod.yml exec redis redis-cli ping
```
- [ ] Redis 响应 PONG

### 6. 后端日志检查
```bash
docker compose -p xiyiyun -f docker-compose.prod.yml logs backend | tail -50
```
检查日志中是否有：
- [ ] 无严重错误（ERROR）
- [ ] 数据库连接成功
- [ ] Redis 连接成功
- [ ] 应用启动成功

---

## 🔧 常见问题排查

### 问题 1: 容器无法启动
```bash
# 查看详细日志
docker compose -p xiyiyun -f docker-compose.prod.yml logs

# 检查配置是否正确
docker compose -p xiyiyun -f docker-compose.prod.yml config

# 重新构建并启动
docker compose -p xiyiyun -f docker-compose.prod.yml up -d --build --force-recreate
```

### 问题 2: 端口被占用
```bash
# 查看端口占用情况
netstat -tuln | grep -E "18080|18081|18088"

# 或使用 lsof
lsof -i :18080
lsof -i :18081
lsof -i :18088
```

### 问题 3: 数据库连接失败
```bash
# 检查 MySQL 容器状态
docker compose -p xiyiyun -f docker-compose.prod.yml ps mysql

# 查看 MySQL 日志
docker compose -p xiyiyun -f docker-compose.prod.yml logs mysql

# 进入 MySQL 容器
docker compose -p xiyiyun -f docker-compose.prod.yml exec mysql bash
```

### 问题 4: 后端 API 无响应
```bash
# 查看后端日志
docker compose -p xiyiyun -f docker-compose.prod.yml logs backend

# 重启后端服务
docker compose -p xiyiyun -f docker-compose.prod.yml restart backend

# 检查后端容器内部
docker compose -p xiyiyun -f docker-compose.prod.yml exec backend bash
```

---

## 🌐 HTTPS 反向代理配置

由于服务绑定在 127.0.0.1，需要配置反向代理才能从外部访问。

### 使用 Nginx 配置示例

创建 `/etc/nginx/sites-available/xiyiyun.conf`:

```nginx
# Web 前台
server {
    listen 80;
    server_name shop.yourdomain.com;

    location / {
        proxy_pass http://127.0.0.1:18080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# H5 端
server {
    listen 80;
    server_name h5.yourdomain.com;

    location / {
        proxy_pass http://127.0.0.1:18081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# 管理后台
server {
    listen 80;
    server_name admin.yourdomain.com;

    location / {
        proxy_pass http://127.0.0.1:18088;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

启用配置：
```bash
ln -s /etc/nginx/sites-available/xiyiyun.conf /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx
```

### 配置 HTTPS (使用 Let's Encrypt)
```bash
certbot --nginx -d shop.yourdomain.com -d h5.yourdomain.com -d admin.yourdomain.com
```

---

## 📊 性能监控

### 查看资源使用情况
```bash
# 查看容器资源使用
docker stats

# 查看磁盘使用
df -h

# 查看内存使用
free -h
```

### 查看数据库大小
```bash
docker compose -p xiyiyun -f docker-compose.prod.yml exec mysql \
  mysql -uroot -p -e "SELECT table_schema AS 'Database',
  ROUND(SUM(data_length + index_length) / 1024 / 1024, 2) AS 'Size (MB)'
  FROM information_schema.tables
  GROUP BY table_schema;"
```

---

## 🔄 更新部署

### 更新代码
```bash
cd /vol1/1000/docker/xiyiyun
git pull

# 如果有数据库迁移（按顺序执行 002~007）
docker compose -p xiyiyun -f docker-compose.prod.yml exec -T mysql \
  sh -lc 'mysql -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" "$MYSQL_DATABASE"' \
  < db/migrations/002_config_persistence.sql
docker compose -p xiyiyun -f docker-compose.prod.yml exec -T mysql \
  sh -lc 'mysql -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" "$MYSQL_DATABASE"' \
  < db/migrations/003_category_icons.sql
docker compose -p xiyiyun -f docker-compose.prod.yml exec -T mysql \
  sh -lc 'mysql -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" "$MYSQL_DATABASE"' \
  < db/migrations/004_security_and_order_consistency.sql
docker compose -p xiyiyun -f docker-compose.prod.yml exec -T mysql \
  sh -lc 'mysql -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" "$MYSQL_DATABASE"' \
  < db/migrations/005_cards_kind_and_callback_logs.sql
docker compose -p xiyiyun -f docker-compose.prod.yml exec -T mysql \
  sh -lc 'mysql -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" "$MYSQL_DATABASE"' \
  < db/migrations/006_money_integrity.sql
docker compose -p xiyiyun -f docker-compose.prod.yml exec -T mysql \
  sh -lc 'mysql -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" "$MYSQL_DATABASE"' \
  < db/migrations/007_config_tables_extraction.sql

# 重新构建并启动
docker compose -p xiyiyun -f docker-compose.prod.yml up -d --build
```

### 备份数据库
```bash
# 备份
docker compose -p xiyiyun -f docker-compose.prod.yml exec -T mysql \
  mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" xiyiyun > backup_$(date +%Y%m%d_%H%M%S).sql

# 恢复
docker compose -p xiyiyun -f docker-compose.prod.yml exec -T mysql \
  mysql -uroot -p"$MYSQL_ROOT_PASSWORD" xiyiyun < backup_20260602_120000.sql
```

---

## 📞 紧急操作

### 停止所有服务
```bash
docker compose -p xiyiyun -f docker-compose.prod.yml down
```

### 完全重置（删除所有数据）
```bash
docker compose -p xiyiyun -f docker-compose.prod.yml down -v
```

### 重启单个服务
```bash
docker compose -p xiyiyun -f docker-compose.prod.yml restart backend
docker compose -p xiyiyun -f docker-compose.prod.yml restart mysql
```

---

## ✅ 最终检查清单

部署完成后，确认以下所有项：

- [ ] 所有容器都在运行
- [ ] 所有端口都在监听
- [ ] Web 前台可以访问
- [ ] H5 可以访问
- [ ] 管理后台可以访问
- [ ] 可以登录管理后台
- [ ] 数据库连接正常
- [ ] Redis 连接正常
- [ ] 日志中无严重错误
- [ ] HTTPS 反向代理已配置（生产环境）
- [ ] CORS 域名已配置为实际域名
- [ ] 管理员密码已修改
- [ ] 数据库已备份

---

**检查脚本**: `scripts/check-server-status.sh`
**服务器信息**: `.server.local.env`
**部署文档**: `docs/FNOS_DEPLOYMENT.md`
